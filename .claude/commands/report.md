Call `get_session_report` and summarise the session metrics for me.

Include:
- Duration and compactions survived
- Files touched
- Tool call breakdown (which tools used, how often)
- Estimated tokens saved
